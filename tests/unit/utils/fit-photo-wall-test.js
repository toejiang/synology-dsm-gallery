import fitPhotoWall from 'gallery/utils/fit-photo-wall';
import { module, test } from 'qunit';

module('Unit | Utility | fit photo wall');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fitPhotoWall();
  assert.ok(result);
});
