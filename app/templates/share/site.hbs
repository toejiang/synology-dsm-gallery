{{#if model}}
  {{gallery-share-path site=model.site}}

  {{#photo-swipe items=model.items as |img|}}
    {{#link-to 'share/detail' model.site img.info.additional.public_share.shareid}}
      <img src={{img.src}} alt={{img.title}} />
    {{/link-to}}
  {{/photo-swipe}}

{{/if}}
{{outlet}}
