{{#if model.items}}
  {{#photo-swipe items=model.items as |img|}}
    {{#link-to 'share/detail' img.info.additional.public_share.shareid}}
      <img src={{img.src}} alt={{img.title}} />
    {{/link-to}}
  {{/photo-swipe}}
{{/if}}
{{outlet}}
